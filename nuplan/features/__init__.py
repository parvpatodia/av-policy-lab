"""nuPlan feature extractors (F0+). Importing this package pulls in numpy only;
all nuPlan-devkit imports inside scene_features are function-local so the pure
geometry/normalization core is usable (and unit-testable) without the devkit."""
