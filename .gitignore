# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
# *.ipynb — notebooks are tracked (removed blanket ignore)

# Data & datasets (large binaries)
*.zip
*.gz
*.tar
*.tar.gz
data/
MNIST/raw/
MNIST/extracted/

# macOS
.DS_Store
*.localized

# Editor
.idea/
.vscode/
*.swp

# Logs / outputs
*.log
wandb/
runs/
outputs/
checkpoints/
# Allow PNGs inside checkpoints (loss curves, trajectory plots)
!nuplan/checkpoints/*.png

# nuPlan simulation artifacts (1GB+ of binary msgpack logs — regenerated by eval scripts)
nuplan/sim_results/
# eval_results: keep the small JSON (consumed by statistical_analysis.py), drop large PNGs
nuplan/eval_results/*.png
